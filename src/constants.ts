
export enum TableName {
  PRODUCT = 'Product',
  Category = 'Category',
  Subcategory = 'Subcategory',
  Grid = 'grids',
}

export const withoutTimestamps = {
  timestamps: false,
  // createdAt: false,
  // deletedAt: false,
  // updatedAt: false,
};
